/* Generated by materialize_syscall_batch.py */
#include <errno.h>
#include <stdio.h>
#include <sys/socket.h>

int main(void)
{
	errno = 0;
	char b; ssize_t r = recvfrom(-1, &b, 1, 0, NULL, NULL);
	int e = errno;
	dprintf(1, "CASE recvfrom.badfd ret=%ld errno=%d note=handwritten\n", (long)r, e);
	return 0;
}

